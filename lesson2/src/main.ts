let myName: string = "dantr";
let backy: number;
let isLoading: boolean;
let album: string | number;

myName = "hndt";
backy = 79;
isLoading = true;
album = "1999";
const sum = (a: number, b: string) => a + b;
console.log(sum(backy, myName));
let postId: string | number;
let isActive: number | boolean | string;
let re:RegExp = /\w+/g;
